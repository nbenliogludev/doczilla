server {
  port = 8080
  uploadDir = "data/uploads"
  retentionDays = 30
}

db {
  url  = "jdbc:h2:mem:doczilla;MODE=PostgreSQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1"
  user = "doczilla"
  pass = "doczilla"
}

auth {
  bootstrap.username = ${?ADMIN_USER}
  bootstrap.password = ${?ADMIN_PASS}
}